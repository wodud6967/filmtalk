
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accordion Menu</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin/header.css">
    <style>

        /* Sidebar styles */
        aside {
            width: 250px;
        }
        aside .side__icon {
            font-weight: bold;
            font-size: 18px;
            margin-bottom: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        aside nav ul {
            margin-bottom: 20px;
        }
        aside nav ul li {
            margin-bottom: 10px;
        }
        aside nav > ul> li> a {
            display: block;
            background-color: #f6f6f6;
            border-top: 1px solid #cfcfcf;
            padding: 0 0 0 17px;
            font-size: 12px;
            line-height: 2.8;
            font-weight: 600;
            vertical-align: middle;
            cursor: pointer;
        }
        aside nav ul li ul {
            display: none;
            margin-top: 10px;
        }
        aside nav ul li ul.on {
            display: block;
        }
        aside nav ul li ul li {
            background-color: #fff;
        }
        aside nav ul li ul li.active a{
            color: red;
        }
        aside nav ul li ul li a {
            font-size: 13px;
            color: #555;
            background-color: #fff;
        }
        aside nav ul li ul li a:hover {
            background-color: #eaeaea;
        }

        main {
            display: flex;
        }
        main section {
            flex: 1;
            min-height: 700px;
            border-left: 1px solid #ccc;
            padding-bottom: 100px;
            background-color: #ccc;

        }
        .sec__top{
            background-color: #fff;
        }
        .content__route{
            border-bottom: 1px solid #d7d7d7;
            margin-left: 16px;
            padding-left: 24px;
            color: #666;
            background: #fff;
            line-height: 28px;
            display: flex;
            justify-content: flex-start;
            gap: 20px;
        }
        .content__route div{
            font-size: 11px;
        }
        .content__title{
            position: relative;
            height: 56px;

            margin-left: 16px;
            padding-left: 24px;
            background: #fff;
            display: flex;
            align-items: center;
        }
        table{
            width: 100%;
            border-collapse: collapse;
            border-spacing: 0;
            border: 1px solid #d7d7d7;
        }
        .fb__filter__title{
            padding-left: 35px;
            color: #000;
            font-size: 13px;
            font-weight: normal;
            text-align: left;
            background: #f9f9f9;
            border-right: 1px solid #d7d7d7;;
        }
        .name__filter__text{
            width: 360px;
            height: 30px;
            margin-right: 10px;
            padding: 5px;
            border: 1px solid #d7d7d7;
            box-sizing: border-box;
        }
        .cinema {
            margin: 20px;
            background-color: #fff;
            padding: 20px;
            width: 60%;
            border: 1px solid #d7d7d7;

        }
        .cinema__list {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr 1fr;
            gap: 20px;
        }
        .screen-item {
            margin-bottom: 15px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            text-align: center;
        }

        .screen-item img {
            width: 100%;
            height: auto;
        }

        .screen-item h5 {
            margin: 10px 0;
        }

        .screen-actions {
            margin-top: 10px;
            display: flex;
            justify-content: center;
        }

        /**/

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        .search-bar {
            display: flex;
            align-items: center;
        }
        .search-bar input[type="text"] {
            width: 300px;
            padding: 8px;
            border: 1px solid #d7d7d7;
            border-radius: 4px;
            margin-right: 10px;
        }
        .search-bar button {
            background-color: #6200ee;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .action-buttons {
            display: flex;
            gap: 10px;
        }
        .action-buttons button {
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .action-buttons .primary {
            background-color: #6200ee;
            color: white;
        }
        .action-buttons .disabled {
            background-color: #e0e0e0;
            color: #a0a0a0;
            cursor: not-allowed;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table th, table td {
            border: 1px solid #d7d7d7;
            padding: 10px;
            text-align: center;
        }
        table th {
            background-color: #f7f7f7;
        }
        .status-label {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 20px;
            color: white;
            font-weight: bold;
        }
        .status-user {
            background-color: #b2dbb2; /* Green background for status */
        }
        .status-admin {
            background-color: #add8e6; /* Light blue background for status */
        }
        .status-test {
            background-color: #a6a6a6; /* Gray background for status */
        }
        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 34px;
            height: 20px;
        }
        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }
        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            transition: 0.4s;
            border-radius: 34px;
        }
        .slider:before {
            position: absolute;
            content: "";
            height: 14px;
            width: 14px;
            left: 3px;
            bottom: 3px;
            background-color: white;
            transition: 0.4s;
            border-radius: 50%;
        }
        input:checked + .slider {
            background-color: #6200ee;
        }
        input:checked + .slider:before {
            transform: translateX(14px);
        }
        .edit-btn {
            background-color: #fff;
            border: 1px solid #ccc;
            padding: 5px 10px;
            cursor: pointer;
        }
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
        }
        .pagination select {
            padding: 5px;
        }

        /* 모달 스타일 */
        .modal {
            display: none;
            position: fixed;
            z-index: 100;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background-color: white;

            border-radius: 8px;
            width: 80%;
            max-width: 1200px;
            position: relative;
        }

        /* 모달 헤더 스타일 */
        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;

            padding: 16px 24px;
            border-bottom: 1px solid #d7d7d7;
        }

        h3 {
            margin: 0;
        }

        .close-btn {
            font-size: 24px;
            cursor: pointer;
            font-weight: bold;
        }

        /* 그리드 레이아웃 */
        .container__seat {
            display: grid;
            grid-template-columns:  3fr 4fr 2fr;
            height: 500px; /* 그리드 컨테이너 높이를 100%로 설정 */

        }
        .container__seat > div{
            border-right: 1px solid #d7d7d7;
            padding: 15px 30px;
            /* 내용이 많아지면 스크롤 */
            overflow-y: auto; /* 개별적으로 스크롤이 생기도록 */
            max-height: 500px; /* 최대 높이를 설정하여 초과할 경우 스크롤 발생 */


        }
        .container__seat > div:first-child{
            border-left: 1px solid #d7d7d7;
        }

        .sidebar {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .tab {
            padding: 10px;
            background-color: #f0f0f0;
            border: 1px solid #ddd;
            cursor: pointer;
            text-align: center;
        }

        .tab.active {
            background-color: #6200ee;
            color: white;
        }

        .schedule-info, .settings, .schedule-list {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .form-group {
            margin-bottom: 16px;
        }

        .form-group label {
            display: block;

            font-size: 14px;
            font-weight: bold;
        }

        .form-group input, .form-group select {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .list-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .list-item img {
            width: 60px;
            height: 60px;
            object-fit: cover;
        }
        .modal-bottom{
            display: flex;
            justify-content: flex-end;
            border-top: 1px solid #d7d7d7;
            padding: 16px 24px;
        }
        .save-button {
            grid-column: span 4;
            padding: 12px;
            background-color: #6200ee;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-align: center;
        }
        .close-button {
            grid-column: span 4;
            padding: 12px;
            background-color: #fff;

            border: 1px solid #d7d7d7;
            border-radius: 4px;
            cursor: pointer;
            text-align: center;
        }
        #seat__create{

        }
        .upload-box {
            width: 200px; /* 원하는 크기로 설정 */
            height: 200px;
            border: 1px dashed #ccc;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            cursor: pointer;
            position: relative;
            transition: background-color 0.3s ease;
        }

        .upload-box:hover {
            background-color: #f0f0f0; /* 마우스 오버 시 배경색 변경 */
        }
        .approval{
            display: flex;
            align-items: center;
            gap: 10px;
        }


    </style>
</head>
<body>

<header>
    <div class="header__top">
        <h1><img src="" alt=""><a href="/admin/dashboard">Admin</a></h1>
        <ul>
            <li><a href="#">서면롯데시네마</a></li>
            <li><a href="#">메인페이지</a></li>
            <li><a href="#">로그아웃</a></li>
        </ul>
    </div>
    <div class="header__bottom">
        <ul>
            <li><a href="/admin/dashboard">데시보드</a> </li>
            <li class="active"><a href="/admin/member">회원관리</a></li>
            <li><a href="/admin/cinema">상영관등록</a></li>
            <li ><a href="/admin/movie">영화관리</a></li>
            <li><a href="/admin/showtime/12">상영관관리</a></li>
            <li><a href="#">영화관관리</a></li>
        </ul>
    </div>
</header>

<main>
    <aside>
        <div class="side__icon">
            <div>회원관리</div>
        </div>

        <nav>
            <ul class="admin__info">
                <li>
                    <a href="#">영화등록</a>
                    <ul >
                        <li><a href="#">최신영화</a></li>
                        <li><a href="#">보유중인영화</a></li>
                        <li><a href="#">상영중인영화</a></li>
                        <li ><a href="#">영화배급받기</a></li>
                        <li><a href="#">영화등록하기</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="point">
                <li>
                    <a href="#">상영관관리</a>
                    <ul >
                        <li class="active"><a href="#">상영관등록</a></li>
                        <li><a href="#">상영관스케줄등록</a></li>
                        <li><a href="#">좌석등록</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="user">
                <li>
                    <a href="#">회원관리하기</a>
                    <ul class="on">
                        <li class="active"><a href="#">회원등록</a></li>
                        <li><a href="#">상영관스케줄등록</a></li>
                        <li><a href="#">좌석등록</a></li>
                    </ul>
                </li>
            </ul>
        </nav>
    </aside>

    <section>
        <div class="sec__top">
            <div class="content__route">
                <div>회원관리</div>
                <div>></div>
                <div>회원등록하기</div>
            </div>
            <div class="content__title">
                <h3>회원등록하기</h3>
            </div>
        </div>
        <div class="cinema">

            <!-- 검색 및 신규 회원 등록 버튼 -->
            <div class="top-bar">
                <div class="search-bar">
                    <input type="text" placeholder="사용자를 검색합니다">
                    <button>🔍</button>
                </div>
                <div class="action-buttons">
                    <button class="primary" id="openModalBtn">회원 등록하기</button>
                    <button class="disabled" disabled>회원레벨변경</button>
                    <button class="disabled" disabled>승인설정변경</button>
                </div>
            </div>

            <!-- 사용자 목록 테이블 -->
            <table>
                <thead>
                <tr>
                    <th>선택</th>
                    <th>영화관 ID</th>
                    <th>이름</th>
                    <th>이메일</th>
                    <th>전화번호</th>
                    <th>직급</th> <!-- 한글로 변환된 직급 -->
                    <th>프로필</th>
                    <th>승인 여부</th>
                    <th>편집</th>
                </tr>
                </thead>
                <tbody>
                {{#models}}
                    {{#adminList}}
                        <tr>
                            <td><input type="checkbox"></td>
                            <td>{{cinemaId}}</td>
                            <td>{{name}}</td>
                            <td>{{email}}</td>
                            <td>{{phone}}</td>
                            <td>{{role}}</td> <!-- 한국어로 변환된 역할 -->
                            <td><span class="status-label status-user">{{name}}</span></td>
                            <td>
                                <label class="toggle-switch">
                                    <input type="checkbox" {{#approved}}checked{{/approved}}>
                                    <span class="slider"></span>
                                </label>
                            </td>
                            <td><button class="edit-btn">✏️</button></td>
                        </tr>
                    {{/adminList}}
                {{/models}}
                </tbody>
            </table>

            <div class="pagination">
                <button disabled>&lt;</button>
                <span>1</span>
                <button>&gt;</button>
                <select>
                    <option value="10">10 / page</option>
                    <option value="20">20 / page</option>
                    <option value="30">30 / page</option>
                </select>
            </div>





        </div>
    </section>


</main>
<!--모달-->
<!-- 모달 창 -->
<div id="addScreenModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3>회원 등록하기</h3>
            <span class="close-btn">&times;</span>

        </div>
        <div class="container__seat">



            <!-- 두 번째 범위: 스케줄 정보 입력 -->
            <div class="schedule-info">
                <div class="form-group admin__check">
                    <label for="admin__id">아이디</label>
                    <input type="text" id="admin__id" placeholder="이메일 입력" />
                </div>
                <div class="form-group">
                    <label for="admin__pw">비밀번호</label>
                    <input type="text" id="admin__pw" placeholder="비밀번호 입력" />
                    <input type="text" placeholder="비밀번호 확인" />
                </div>
                <div class="form-group">
                    <label for="admin__name">이름</label>
                    <input type="text" id="admin__name" placeholder="이름 입력" />
                </div>
                <div class="form-group">
                    <label for="department">부서</label>
                    <input type="text" id="department" placeholder="부서 입력" />
                </div>
                <div class="form-group">
                    <label for="phone">전화번호</label>
                    <input type="text" id="phone" placeholder="전화번호 입력" />
                </div>
                <div class="form-group">
                    <label for="email">이메일</label>
                    <input type="text" id="email" placeholder="이메일 입력" />
                </div>
                <div class="form-group">
                    <label for="adr">주소</label>
                    <input type="text" id="adr" placeholder="주소 입력" />
                </div>

            </div>

            <!-- 세 번째 범위: 스케줄 편성 리스트 -->
            <div class="schedule-list">
                <div class="form-group">
                    <label for="profile">프로필이미지</label>
                    <div class="upload-box">
                        <p>jps 또는 png형식</p>
                    </div>
                </div>




            </div>
            <!-- 세 번째 범위: 스케줄 편성 리스트 -->
            <div>
                <div class="form-group approval">
                    <label class="form-check-label" for="approvalSwitch">승인 여부</label>

                    <div class="form-check form-switch">
                        <input class="form-check-input" type="checkbox" id="approvalSwitch">
                    </div>
                </div>

                <!-- 관리자 권한 라디오 버튼 -->
                <div class="form-group">
                    <label class="form-check-label">관리자 권한</label>

                </div>

                <div class="form-check">
                    <input class="form-check-input" type="radio" name="adminRole" id="accessRole" value="access" checked>
                    <label class="form-check-label" for="accessRole">
                        접근 권한
                    </label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="adminRole" id="normalRole" value="normal">
                    <label class="form-check-label" for="normalRole">
                        일반 사용자
                    </label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="adminRole" id="superRole" value="super">
                    <label class="form-check-label" for="superRole">
                        상위 관리자
                    </label>
                </div>
            </div>


        </div>
        <div class="modal-bottom">
            <div class="bottom-right">
                <button class="save-button">저장</button>
                <button class="close-button">취소</button>
            </div>
        </div>

    </div>
</div>


<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Get all list items with submenus
    const menuItems = document.querySelectorAll('aside nav ul li a');

    // Loop through all the menu items and add click event listeners
    menuItems.forEach(item => {
        item.addEventListener('click', (e) => {
            // Prevent default behavior of the link
            e.preventDefault();

            // Get the next sibling (which should be the submenu)
            const submenu = item.nextElementSibling;

            // Toggle the "on" class to show or hide the submenu
            if (submenu.classList.contains('on')) {
                submenu.classList.remove('on');
            } else {
                submenu.classList.add('on');
            }
        });
    });




</script>

<script>
    // 모달 열기와 닫기 기능
    const modal = document.getElementById("addScreenModal");
    const openModalBtn = document.getElementById("openModalBtn");
    const closeModalBtn = document.querySelector(".close-btn");

    openModalBtn.addEventListener("click", () => {
        modal.style.display = "flex";
    });

    closeModalBtn.addEventListener("click", () => {
        modal.style.display = "none";
    });

    window.addEventListener("click", (e) => {
        if (e.target == modal) {
            modal.style.display = "none";
        }
    });
</script>


</body>
</html>