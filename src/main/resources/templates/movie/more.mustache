<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>더보기페이지</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/header.css">
    <style>
        #main {
            width: 100%;
            background-color: aliceblue;
            margin-top: 20px;
        }

        .inner {
            /*width: 1200px;*/
            margin: 0 auto;
            padding: 1px;
        }

        #sec1 {
            color: black;
            background-color: #fff8ec;
            /*height: 400px;*/
        }

        #sec2 {
            color: black;
            background-color: #e2f8ff;
            /*height: 400px;*/
        }

        #sec3 {
            color: black;
            background-color: #f4ffe9;
            /*height: 400px;*/
        }
        .movie-grid {
            display: grid;
            grid-template-columns: repeat(5, 1fr); /* 5개의 칸을 자동으로 생성 */
            gap: 20px; /* 각 카드 간의 간격 */
        }


        .movie-card {
            position: relative;
            margin-bottom: 20px;
        }

        .movie-card img {
            width: 100%;
            height: auto;
        }

        .badge-custom {
            position: absolute;
            top: 10px;
            left: 10px;
        }

        .movie-info {
            text-align: center;
            margin-top: 10px;
        }

        .movie-title {
            font-size: 0.9rem;
            font-weight: bold;
        }

        .movie-runtime {
            font-size: 0.7rem;
            color: #888;
        }
    </style>
</head>
<body>

{{>layout/header}}

<!-- 메인에서 작업하면되 1200px 중앙정렬-->
<main id="main">
    <section id="sec1" class="inner">
        <div class="container mt-5">
            <!-- 현재 상영작 TOP 5 -->
            <div class="row mb-4">
                <div class="col-12 d-flex justify-content-between align-items-center">
                    <h4 class="fw-bold">현재 상영작 전체</h4>
                    <a class="text-muted" href="/">홈으로</a>
                </div>
            </div>
            <!-- 영화 카드 Grid 레이아웃 -->
            <div class="movie-grid">
                {{#models}}
                    <div class="movie-card">
                        <a href="movie/{{id}}">
                            <img src="{{posterUrl}}" alt="영화 포스터">
                            <span class="badge bg-success badge-custom">{{ratingGrade}}</span>
                        </a>

                        <div class="row mb-2">
                            <div class="movie-info">
                                <div class="movie-title">{{movieNm}}</div>
                                <div class="movie-runtime">{{runtime}}분</div>
                                <button onclick="go__reservaion()" class="btn btn-primary btn-sm mt-2">예매하기</button>
                            </div>
                        </div>
                    </div>
                {{/models}}
            </div>
        </div>
    </section>


    </section>

</main>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<script>

    function go__reservaion(){
        window.location.href = "/reservation";
    }

    $(".reservation__btn").on("click", function() {
        window.location.href = "/reservation";
    });
</script>



{{>layout/footer}}
